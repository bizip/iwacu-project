<%- include("../includes/header.ejs") %>
    <h2>Here is the list af all the products</h2>
    <a href="/add-product">New Product</a>
    <% if(prods.length>0) {%>
        <% for(let product of prods){%>
            <fieldset>
                <legend>House location</legend>
                <ul>
                    <li>
                        <%= product.title %>
                    </li>

                </ul>
            </fieldset>
            <ul>
                <li>
                    <%= product.title %>&nbsp;&nbsp;

                </li>
            </ul>
            <%}%>
                <%}else{ %>
                    <h2>product not found</h2>
                    <%}%>
                        <%- include("../includes/footer.ejs") %>